version: '3.8'

services:
  terraform:
    image: hashicorp/terraform:1.13.4
    volumes:
      - ./terraform:/terraform
    env_file: ".env"
    working_dir: /terraform
    entrypoint: ["terraform"]
