# Optional AWS credentials for Terraform and AWS CLI commands.
# The local Docker stack reads safe defaults from docker-compose.yaml.
# Override local Docker variables with shell exports when needed, for example:
# export WORDPRESS_SITE_TITLE="Cloud-1 Local"
# export WORDPRESS_ADMIN_PASSWORD="adminpassword"
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
